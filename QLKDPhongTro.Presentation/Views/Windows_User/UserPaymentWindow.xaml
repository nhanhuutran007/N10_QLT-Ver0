<Window x:Class="QLKDPhongTro.Presentation.Views.Windows_User.UserPaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:QLKDPhongTro.Presentation.ViewModels"
        xmlns:components="clr-namespace:QLKDPhongTro.Presentation.Views.Components"
        xmlns:converters="clr-namespace:QLKDPhongTro.Presentation.Converters"
        mc:Ignorable="d"
        Title="Thanh toán - Quản Lý Phòng Trọ"
        MinHeight="600"
        MinWidth="800"
        Height="700"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F5F5F5"
        AllowsTransparency="False"
        FontFamily="Roboto"
        Icon="/Resources/Images/Logo.png">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BooleanToVisibilityInverseConverter x:Key="BooleanToVisibilityInverseConverter"/>
        
        <!-- Style cho TextBox với hiệu ứng focus -->
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F2F6FA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#3B4758"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#319DFF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" Color="#319DFF" Opacity="0.3" BlurRadius="5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho tiêu đề section -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#3B4758"/>
            <Setter Property="Margin" Value="0,20,0,15"/>
        </Style>

        <!-- Style cho label -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#3B4758"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Style cho card container -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Color="#D6D8E0" Opacity="0.3" BlurRadius="10"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern DataGrid Style -->
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowHeight" Value="60"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- DataGrid Column Header Style -->
        <Style x:Key="ModernColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <!-- DataGrid Row Style -->
        <Style x:Key="ModernRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F1F5F9"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8FAFC"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="BorderBrush" Value="#BFDBFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="ModernCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#1E40AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Window.DataContext>
        <vm:UserPaymentViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="269" MinWidth="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- === SIDEBAR CONTROL === -->
        <components:UserSidebarControl Grid.Column="0" x:Name="SidebarControl"/>

        <!-- === MAIN CONTENT === -->
        <Grid Margin="223,15,15,15" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar Control -->
            <components:TopbarControl Grid.Row="0" x:Name="TopbarControl" 
                                     VerticalAlignment="Top" HorizontalAlignment="Stretch"/>

            <!-- Loading State -->
            <Grid Grid.Row="1" 
                  Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" 
                                Width="200" 
                                Height="4"
                                Foreground="#319DFF"/>
                    <TextBlock Text="Đang tải dữ liệu thanh toán..."
                              FontSize="14"
                              Foreground="#64748B"
                              Margin="0,15,0,0"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- No Data State -->
            <Grid Grid.Row="1" 
                  Visibility="{Binding HasData, Converter={StaticResource BooleanToVisibilityInverseConverter}}">
                <StackPanel HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityInverseConverter}}">
                    <Path Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4M11,9V11H13V9H11M11,13V17H13V13H11Z"
                          Fill="#CBD5E1"
                          Width="64"
                          Height="64"
                          Stretch="Uniform"
                          HorizontalAlignment="Center"/>
                    <TextBlock Text="Không tìm thấy thông tin thanh toán"
                              FontSize="16"
                              FontWeight="SemiBold"
                              Foreground="#475569"
                              Margin="0,20,0,5"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="Tài khoản của bạn chưa có hóa đơn thanh toán nào."
                              FontSize="14"
                              Foreground="#94A3B8"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Content: 2-Panel Layout -->
            <Grid Grid.Row="1" 
                  Visibility="{Binding HasData, Converter={StaticResource BooleanToVisibilityConverter}}"
                  Margin="10,10,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT PANEL: Payment List -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- List Header -->
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="Lịch sử thanh toán" 
                                       FontSize="18" 
                                       FontWeight="Bold" 
                                       Foreground="#1E293B"
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="Danh sách các hóa đơn của bạn"
                                       FontSize="12"
                                       Foreground="#94A3B8"
                                       Margin="0,0,0,15"/>
                        </StackPanel>

                        <!-- Deposit Info Card -->
                        <Border Grid.Row="1" 
                                CornerRadius="10"
                                Padding="15"
                                Margin="0,0,0,15">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#667EEA" Offset="0"/>
                                    <GradientStop Color="#764BA2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <StackPanel>
                                <TextBlock Text="💰 Tiền cọc" 
                                          FontSize="12" 
                                          Foreground="White" 
                                          Opacity="0.9"
                                          Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding TienCocBanDau}" 
                                          FontSize="20" 
                                          FontWeight="Bold" 
                                          Foreground="White"
                                          Margin="0,0,0,5"/>
                                <TextBlock Text="Tiền cọc ban đầu" 
                                          FontSize="11" 
                                          Foreground="White" 
                                          Opacity="0.8"/>
                            </StackPanel>
                        </Border>

                        <!-- DataGrid -->
                        <DataGrid Grid.Row="2"
                                  ItemsSource="{Binding Payments}"
                                  SelectedItem="{Binding SelectedPayment, Mode=TwoWay}"
                                  Style="{StaticResource ModernDataGridStyle}"
                                  ColumnHeaderStyle="{StaticResource ModernColumnHeaderStyle}"
                                  RowStyle="{StaticResource ModernRowStyle}"
                                  CellStyle="{StaticResource ModernCellStyle}">
                            <DataGrid.Columns>
                                <!-- Month/Year Column with Icon -->
                                <DataGridTemplateColumn Header="Tháng" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Vertical">
                                                <TextBlock Text="{Binding ThangNam}" 
                                                          FontWeight="SemiBold"
                                                          FontSize="13"
                                                          Foreground="#1E293B"/>
                                                <TextBlock Text="{Binding TenPhong}" 
                                                          FontSize="11"
                                                          Foreground="#94A3B8"
                                                          Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Status Column with Badge -->
                                <DataGridTemplateColumn Header="Trạng thái" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding TrangThaiColor}"
                                                   CornerRadius="12"
                                                   Padding="10,5"
                                                   HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding TrangThai}" 
                                                          FontSize="11"
                                                          FontWeight="SemiBold"
                                                          Foreground="White"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- RIGHT PANEL: Payment Details -->
                <ScrollViewer Grid.Column="1" 
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel DataContext="{Binding SelectedPayment}">
                            <!-- Header -->
                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Chi tiết hóa đơn" 
                                               FontSize="22" 
                                               FontWeight="Bold" 
                                               Foreground="#1E293B"/>
                                    <TextBlock Text="{Binding MaThanhToanDisplay}" 
                                               FontSize="14" 
                                               Foreground="#64748B"
                                               Margin="0,5,0,0"/>
                                </StackPanel>
                                <Border Grid.Column="1"
                                       Background="{Binding TrangThaiColor}"
                                       CornerRadius="8"
                                       Padding="15,8"
                                       VerticalAlignment="Top">
                                    <TextBlock Text="{Binding TrangThai}" 
                                              FontSize="13"
                                              FontWeight="Bold"
                                              Foreground="White"/>
                                </Border>
                            </Grid>

                            <!-- Section: Thông tin thanh toán -->
                            <TextBlock Text="📋 Thông tin thanh toán" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Tháng/Năm" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding ThangNam, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Ngày thanh toán" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding NgayThanhToan, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Phòng" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TenPhong, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Người thuê" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TenKhachHang, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>
                            </Grid>

                            <!-- Section: Chi tiết chi phí -->
                            <TextBlock Text="💵 Chi tiết chi phí" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Tiền thuê" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TienThue, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" Foreground="#0EA5E9" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Tiền điện" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TienDien, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" Foreground="#F59E0B" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Tiền nước" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TienNuoc, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" Foreground="#06B6D4" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Tiền Internet" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TienInternet, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Tiền vệ sinh" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TienVeSinh, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Tiền giữ xe" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding TienGiuXe, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
                                    <TextBlock Text="Chi phí khác" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding ChiPhiKhac, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>
                            </Grid>

                            <!-- Section: Điện nước chi tiết -->
                            <TextBlock Text="⚡ Thông tin điện nước" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Chỉ số điện cũ" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding ChiSoDienCu, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Chỉ số điện mới" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding ChiSoDienMoi, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Số điện tiêu thụ (kWh)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding SoDien, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Đơn giá điện" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding DonGiaDien, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,15,15">
                                    <TextBlock Text="Số nước tiêu thụ (m³)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding SoNuoc, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Đơn giá nước" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding DonGiaNuoc, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42"/>
                                </StackPanel>
                            </Grid>

                            <!-- Section: Tổng kết -->
                            <TextBlock Text="💰 Tổng kết thanh toán" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Border Background="#F8FAFC" 
                                   CornerRadius="10" 
                                   Padding="20"
                                   Margin="0,10,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,15">
                                        <TextBlock Text="Tổng tiền" Style="{StaticResource LabelStyle}"/>
                                        <TextBox Text="{Binding TongTien, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" Foreground="#DC2626" FontWeight="Bold" FontSize="15"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                        <TextBlock Text="Đã thanh toán" Style="{StaticResource LabelStyle}"/>
                                        <TextBox Text="{Binding SoTienDaTra, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" Foreground="#16A34A" FontWeight="SemiBold"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2">
                                        <TextBlock Text="Còn lại" Style="{StaticResource LabelStyle}"/>
                                        <TextBox Text="{Binding SoTienConLai, Mode=OneWay}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" Height="42" Foreground="#EA580C" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Section: Ghi chú -->
                            <TextBlock Text="📝 Ghi chú" Style="{StaticResource SectionTitleStyle}"/>
                            <TextBox Text="{Binding GhiChu, Mode=OneWay}" 
                                    Style="{StaticResource InputTextBoxStyle}" 
                                    IsReadOnly="True" 
                                    TextWrapping="Wrap" 
                                    Height="80"
                                    VerticalContentAlignment="Top"
                                    Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
