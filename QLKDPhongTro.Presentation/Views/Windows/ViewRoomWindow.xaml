<Window x:Class="QLKDPhongTro.Presentation.Views.Windows.ViewRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:QLKDPhongTro.Presentation.Converters"
        mc:Ignorable="av"
        Title="Chi tiết phòng"
        Height="680" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Resources/Images/Logo.png">

    <Window.Resources>
        <converters:EmptyToVisibilityConverter x:Key="EmptyToVisibilityConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="CloseButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FEE2E2"/>
                    <Setter Property="Foreground" Value="#EF4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="{x:Type Border}">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedRoom.TrangThai}" Value="Đang thuê">
                    <Setter Property="Background" Value="#D1FAE5"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SelectedRoom.TrangThai}" Value="Trống">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SelectedRoom.TrangThai}" Value="Đang bảo trì">
                    <Setter Property="Background" Value="#FEE2E2"/>
                </DataTrigger>
                <!-- Đổi màu khi có người thuê nhưng chưa có hợp đồng -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding SelectedRoom.TrangThai}" Value="Đang thuê"/>
                        <Condition Binding="{Binding NeedsContractWarning}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FEF3C7"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedRoom.TrangThai}" Value="Đang thuê">
                    <Setter Property="Foreground" Value="#059669"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SelectedRoom.TrangThai}" Value="Trống">
                    <Setter Property="Foreground" Value="#1D4ED8"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SelectedRoom.TrangThai}" Value="Đang bảo trì">
                    <Setter Property="Foreground" Value="#B91C1C"/>
                </DataTrigger>
                <!-- Đổi màu khi có người thuê nhưng chưa có hợp đồng -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding SelectedRoom.TrangThai}" Value="Đang thuê"/>
                        <Condition Binding="{Binding NeedsContractWarning}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Foreground" Value="#92400E"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MaintenanceStatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,2"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThai}" Value="Chưa xử lý">
                    <Setter Property="Background" Value="#FEE2E2"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThai}" Value="Đang xử lý">
                    <Setter Property="Background" Value="#FEF3C7"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThai}" Value="Hoàn tất">
                    <Setter Property="Background" Value="#D1FAE5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="15">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="5" Direction="270" Color="Black" Opacity="0.2" BlurRadius="20"/>
        </Border.Effect>

        <Border CornerRadius="16" Background="White" ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="24,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Chi tiết phòng" FontSize="20" FontWeight="Bold" Foreground="#111827"/>
                        <TextBlock Text="{Binding SelectedRoom.TenPhong, StringFormat='Thông tin chi tiết cho phòng {0}'}" 
                                   FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="✕" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click" VerticalAlignment="Top"/>
                </Grid>

                <Separator Grid.Row="0" VerticalAlignment="Bottom" Background="#F3F4F6" Margin="24,0"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <Border Background="#F9FAFB" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                                <StackPanel>
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="48" Height="48" CornerRadius="10" Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
                                            <TextBlock Text="🏠" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding SelectedRoom.TenPhong}" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <Border Style="{StaticResource StatusBadgeStyle}">
                                                    <TextBlock Text="{Binding SelectedRoom.TrangThai}" Style="{StaticResource StatusTextStyle}"/>
                                                </Border>
                                                <TextBlock Text="{Binding SelectedRoom.MaPhong, StringFormat='ID: {0}'}" Foreground="#9CA3AF" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="GIÁ THUÊ" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding SelectedRoom.GiaCoBan, StringFormat='{}{0:N0} đ'}" FontSize="16" FontWeight="SemiBold" Foreground="#111827"/>
                                            <TextBlock Text="{Binding SelectedRoom.GiaBangChu}" FontSize="12" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" MinWidth="80">
                                            <TextBlock Text="DIỆN TÍCH" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding SelectedRoom.DienTich, StringFormat='{}{0} m²'}" FontSize="16" FontWeight="SemiBold" Foreground="#111827"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,20" BorderBrush="#E5E7EB" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="NGƯỜI THUÊ" FontSize="12" FontWeight="Bold" Foreground="#4B5563" Margin="0,0,0,12"/>

                                    <ItemsControl ItemsSource="{Binding CurrentTenants}" Margin="0,0,0,0"
                                                  Visibility="{Binding HasCurrentTenants, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel IsItemsHost="True"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="250"
                                                        Background="#F9FAFB"
                                                        CornerRadius="12"
                                                        BorderBrush="#E5E7EB"
                                                        BorderThickness="1"
                                                        Padding="12"
                                                        Margin="0,0,12,12">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding HoTen}" FontSize="13" FontWeight="SemiBold" Foreground="#111827"/>
                                                            <Border Margin="10,0,0,0" Padding="8,2" CornerRadius="6">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="#E5E7EB"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsContractHolder}" Value="True">
                                                                                <Setter Property="Background" Value="#DCFCE7"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                <TextBlock Text="{Binding RoleLabel}" FontSize="8" FontWeight="SemiBold" Foreground="#374151"/>
                                                            </Border>
                                                        </StackPanel>
                                                        <TextBlock Text="{Binding TrangThaiNguoiThue, StringFormat='Trạng thái khách: {0}'}" Foreground="#4B5563" Margin="0,6,0,0"/>
                                                        <TextBlock Text="{Binding TrangThaiHopDong, StringFormat='Hợp đồng: {0}'}" Foreground="#4B5563" Margin="0,2,0,0"/>
                                                        <TextBlock Text="{Binding ContractRangeDisplay}" Foreground="#6B7280" Margin="0,2,0,0"/>
                                                        <TextBlock Text="{Binding SoDienThoai, StringFormat='Điện thoại: {0}'}" Foreground="#6B7280" Margin="0,2,0,0"
                                                                   Visibility="{Binding SoDienThoai, Converter={StaticResource EmptyToVisibilityConverter}, ConverterParameter=inverse}"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="Chưa có dữ liệu người thuê." Margin="0,12,0,0" FontStyle="Italic" Foreground="#9CA3AF"
                                               Visibility="{Binding HasCurrentTenants, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"/>
                                    
                                    <!-- Cảnh báo khi có người thuê nhưng chưa có hợp đồng -->
                                    <Border Background="#FEF3C7" CornerRadius="8" Padding="12" Margin="0,12,0,0" BorderBrush="#FCD34D" BorderThickness="1"
                                            Visibility="{Binding NeedsContractWarning, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <StackPanel>
                                            <TextBlock Text="⚠️ CẦN TẠO HỢP ĐỒNG" FontSize="12" FontWeight="Bold" Foreground="#92400E" Margin="0,0,0,4"/>
                                            <TextBlock Text="Phòng này đã có người thuê nhưng chưa có hợp đồng. Vui lòng tạo hợp đồng để quản lý chính thức." 
                                                       FontSize="11" Foreground="#78350F" TextWrapping="Wrap" LineHeight="16"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="TIỆN NGHI" FontSize="12" FontWeight="Bold" Foreground="#4B5563" Margin="0,0,0,12"/>
                                    <ItemsControl ItemsSource="{Binding SelectedRoomAmenities}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#F3F4F6" CornerRadius="15" Padding="12,6" Margin="0,0,8,8">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="✓" Foreground="#10B981" FontWeight="Bold" Margin="0,0,6,0"/>
                                                        <TextBlock Text="{Binding}" FontSize="13" Foreground="#374151"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="Chưa cập nhật tiện nghi" FontStyle="Italic" Foreground="#9CA3AF" 
                                               Visibility="{Binding SelectedRoomAmenities.Count, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                                </StackPanel>
                            </Border>

                            <StackPanel>
                                <TextBlock Text="GHI CHÚ" FontSize="12" FontWeight="Bold" Foreground="#4B5563" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding SelectedRoom.GhiChu, FallbackValue='Không có ghi chú'}" 
                                           FontSize="14" Foreground="#4B5563" TextWrapping="Wrap" LineHeight="20"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <Border BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="12" Padding="0">
                                <StackPanel>
                                    <Border Background="#F9FAFB" CornerRadius="12,12,0,0" Padding="16,12" BorderThickness="0,0,0,1" BorderBrush="#E5E7EB">
                                        <Grid>
                                            <TextBlock Text="🔧 TRẠNG THÁI &amp; BẢO TRÌ" FontSize="12" FontWeight="Bold" Foreground="#4B5563" VerticalAlignment="Center"/>
                                            <Border Background="#FEF3C7" CornerRadius="10" Padding="8,2" HorizontalAlignment="Right" 
                                                    Visibility="{Binding OpenMaintenanceCount, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Text="{Binding OpenMaintenanceCount, StringFormat='{}{0} Mở'}" FontSize="11" FontWeight="Bold" Foreground="#B45309"/>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto" Padding="16">
                                        <StackPanel>
                                            <ItemsControl ItemsSource="{Binding SelectedRoomMaintenance}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,0,0,12">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding MoTaSuCo}" FontWeight="Medium" TextWrapping="Wrap" Foreground="#111827"/>
                                                                <TextBlock Text="{Binding NgayBaoCao, StringFormat='{}{0:dd/MM/yyyy}'}" FontSize="11" Foreground="#9CA3AF" Margin="0,2,0,0"/>
                                                            </StackPanel>
                                                            <Border Grid.Column="1" Style="{StaticResource MaintenanceStatusBadgeStyle}" VerticalAlignment="Top" Margin="8,0,0,0">
                                                                <TextBlock Text="{Binding TrangThai}" FontSize="11" FontWeight="SemiBold">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding TrangThai}" Value="Chưa xử lý">
                                                                                    <Setter Property="Foreground" Value="#991B1B"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding TrangThai}" Value="Đang xử lý">
                                                                                    <Setter Property="Foreground" Value="#92400E"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding TrangThai}" Value="Hoàn tất">
                                                                                    <Setter Property="Foreground" Value="#065F46"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </Border>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <TextBlock Text="Không có yêu cầu bảo trì." Foreground="#9CA3AF" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,10"
                                                       Visibility="{Binding SelectedRoomMaintenance.Count, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </ScrollViewer>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>

                <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="#F3F4F6" Padding="24,16" Background="#F9FAFB">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Chỉnh sửa" Background="White" Foreground="#374151" BorderBrush="#D1D5DB" BorderThickness="1"
                                Padding="16,8" Margin="0,0,12,0" FontWeight="SemiBold" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Content="Đóng" Click="CloseButton_Click" Background="#319DFF" Foreground="White" BorderThickness="0"
                                Padding="20,8" FontWeight="SemiBold" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>