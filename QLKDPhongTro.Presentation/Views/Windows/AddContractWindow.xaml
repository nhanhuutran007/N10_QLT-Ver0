<!-- Presentation/Views/Windows/AddContractWindow.xaml -->
<Window x:Class="QLKDPhongTro.Presentation.Views.Windows.AddContractWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm hợp đồng" Height="360" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tạo hợp đồng mới" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,4,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">Người thuê:</TextBlock>
                <ComboBox Width="320"
                          ItemsSource="{Binding NguoiThueList}"
                          DisplayMemberPath="HoTen"
                          SelectedItem="{Binding SelectedNguoiThue, Mode=TwoWay}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">Phòng:</TextBlock>
                <ComboBox Width="320"
                          ItemsSource="{Binding PhongList}"
                          DisplayMemberPath="TenPhong"
                          SelectedItem="{Binding SelectedPhong, Mode=TwoWay}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">Ngày bắt đầu:</TextBlock>
                <DatePicker SelectedDate="{Binding NgayBatDau}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">Ngày kết thúc:</TextBlock>
                <DatePicker SelectedDate="{Binding NgayKetThuc}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Center">Tiền cọc:</TextBlock>
                <TextBox Width="200" Text="{Binding TienCoc, Mode=TwoWay}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Width="120" VerticalAlignment="Top">Ghi chú (tùy chọn):</TextBlock>
                <TextBox Width="320" Height="60" Text="{Binding GhiChu, Mode=TwoWay}" AcceptsReturn="True"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Lưu" Width="100" Margin="6,0" IsDefault="True" Command="{Binding SaveCommand}"/>
            <Button Content="Hủy" Width="100" Margin="6,0" IsCancel="True" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
