<Window x:Class="QLKDPhongTro.Presentation.Views.Windows.EditRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm phòng mới"
        Height="650" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#319DFF"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="20">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="0.25" BlurRadius="25"/>
        </Border.Effect>

        <Border Background="White" CornerRadius="16" ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Background="White">
                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="#111827" Margin="24,24,0,0"/>
                    <Button Command="{Binding CancelAddEditCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,20,0"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" Width="32" Height="32">
                        <TextBlock Text="✕" FontSize="18" Foreground="#9CA3AF"/>
                    </Button>
                </Grid>

                <ScrollViewer Grid.Row="1" Margin="0,10,0,0" VerticalScrollBarVisibility="Auto">
                    <Border Padding="24,10,24,24">
                        <StackPanel>
                            <TextBlock Text="Tên phòng" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding NewRoom.TenPhong, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Diện tích (m²)" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
                                    <TextBox Text="{Binding NewRoom.DienTich, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Giá thuê (VNĐ)" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
                                    <TextBox Text="{Binding NewRoom.GiaCoBan, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Giá bằng chữ" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding NewRoom.GiaBangChu, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                            <TextBlock Text="Trạng thái" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding CurrentStatusOptions}" SelectedValue="{Binding NewRoom.TrangThai}" Height="44" 
                                      VerticalContentAlignment="Center" Padding="12,0" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1">
                                <ComboBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>

                            <TextBlock Text="Tiện nghi (cách nhau dấu phẩy)" FontWeight="SemiBold" Foreground="#374151" Margin="0,20,0,8"/>
                            <TextBox Text="{Binding NewRoom.TrangThietBi, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="12,12"/>

                            <TextBlock Text="Ghi chú" FontWeight="SemiBold" Foreground="#374151" Margin="0,20,0,8"/>
                            <TextBox Text="{Binding NewRoom.GhiChu, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="12,12"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>

                <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="#F3F4F6" Background="#F9FAFB" Padding="24,16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Hủy bỏ" Command="{Binding CancelAddEditCommand}"
                                Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Foreground="#374151"
                                Padding="16,10" Margin="0,0,12,0" FontWeight="SemiBold" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="{Binding ButtonContent}" Command="{Binding SaveCommand}"
                                Background="#319DFF" Foreground="White" BorderThickness="0"
                                Padding="24,10" FontWeight="SemiBold" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1F8AF0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>