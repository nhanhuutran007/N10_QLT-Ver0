<Window x:Class="YourApp.OtpLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xác thực OTP - Quản lý Thuê Nhà" 
        Height="810" Width="1440" 
        WindowStartupLocation="CenterScreen"
        FontFamily="Roboto"
        Icon="/Resources/Images/Logo.png">

    <Window.Resources>
        <!-- ... (Các Style không thay đổi) ... -->
        <Style x:Key="OtpTextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="64"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Background" Value="#F2F6FA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#3B4758"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MaxLength" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="border" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#319DFF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" Color="#319DFF" Opacity="0.5" BlurRadius="5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho TextBox thông thường với hiệu ứng focus -->
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F2F6FA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#3B4758"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#319DFF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" Color="#319DFF" Opacity="0.3" BlurRadius="5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="432"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Background" Value="#319DFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#57B3FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#319DFF"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter x:Name="contentPresenter" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="contentPresenter" Property="TextBlock.TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FeatureItemStyle" TargetType="Grid">
            <Setter Property="Margin" Value="0,12"/>
        </Style>
        <Style x:Key="FeatureTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style x:Key="FeatureDescriptionStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="173*" MinWidth="400"/>
            <ColumnDefinition Width="428*"/>
            <ColumnDefinition Width="600*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <!-- === CỘT TRÁI: PHẦN GIỚI THIỆU === -->
        <Grid Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#2764FF" Offset="0.0" />
                    <GradientStop Color="#0B459B" Offset="1.0" />
                </LinearGradientBrush>
            </Grid.Background>

            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="80,0,80,0">
                <Image Source="/Resources/Images/Logo.png" 
                       Width="140" Height="167" />

                <TextBlock Text="Hệ thống Quản lý Thuê Nhà" 
                           Foreground="White" FontSize="30" FontWeight="Bold" 
                           TextWrapping="Wrap" TextAlignment="Left" 
                           Width="429" Height="34"/>

                <StackPanel>
                    <Grid Margin="0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="10" Height="10" Fill="White" Margin="0,6,12,0" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Quản lý phòng &amp; tài sản" Foreground="White" FontWeight="Medium" FontSize="16"/>
                            <TextBlock Text="Theo dõi danh sách nhà, phòng, tình trạng thuê (trống, đang thuê), và thông tin tài sản chi tiết." Foreground="#E0E0E0" FontSize="14" TextWrapping="Wrap" Opacity="0.9"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="10" Height="10" Fill="White" Margin="0,6,12,0" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Quản lý người thuê" Foreground="White" FontWeight="Medium" FontSize="16"/>
                            <TextBlock Text="Lưu thông tin cá nhân, hợp đồng, tình trạng thuê; nhắc nhở khi đến hạn trả phòng hoặc hết hạn hợp đồng." Foreground="#E0E0E0" FontSize="14" TextWrapping="Wrap" Opacity="0.9"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="10" Height="10" Fill="White" Margin="0,6,12,0" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Quản lý tài chính" Foreground="White" FontWeight="Medium" FontSize="16"/>
                            <TextBlock Text="Ghi nhận tiền thuê, chi phí điện nước, internet; tự động tổng hợp công nợ và lợi nhuận hàng tháng." Foreground="#E0E0E0" FontSize="14" TextWrapping="Wrap" Opacity="0.9"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="10" Height="10" Fill="White" Margin="0,6,12,0" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Thanh toán &amp; thông báo phí" Foreground="White" FontWeight="Medium" FontSize="16"/>
                            <TextBlock Text="Tạo biên lai, gửi thông báo phí kèm mã QR, nhắc nợ qua email/Zalo." Foreground="#E0E0E0" FontSize="14" TextWrapping="Wrap" Opacity="0.9"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="10" Height="10" Fill="White" Margin="0,6,12,0" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Bảo trì &amp; sự cố" Foreground="White" FontWeight="Medium" FontSize="16"/>
                            <TextBlock Text="Tiếp nhận yêu cầu sửa chữa, theo dõi tiến độ và chi phí bảo trì." Foreground="#E0E0E0" FontSize="14" TextWrapping="Wrap" Opacity="0.9"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="10" Height="10" Fill="White" Margin="0,6,12,0" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Báo cáo &amp; thống kê" Foreground="White" FontWeight="Medium" FontSize="16"/>
                            <TextBlock Text="Xuất báo cáo chi tiết về doanh thu, công nợ, tình trạng phòng, hỗ trợ quyết định kinh doanh." Foreground="#E0E0E0" FontSize="14" TextWrapping="Wrap" Opacity="0.9"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- === CỘT PHẢI: FORM ĐĂNG NHẬP OTP === -->
        <Grid Grid.Column="2" Background="#F9FAFB">
            <StackPanel Width="400" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                <!-- Tiêu đề Form -->
                <TextBlock Text="Đăng nhập" FontSize="24" FontWeight="Bold" Foreground="#3B4758" Margin="0,0,0,30"/>

                <!-- Hướng dẫn và Email -->
                <TextBlock Foreground="#586A84" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,5"><Run Text="Nhập mã 6 chữ số bạn nhận được qua địa chỉ"/><LineBreak/><Run Text="bên dưới của bạn."/></TextBlock>

                <!-- Các ô nhập mã OTP -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,30">
                    <TextBox Style="{StaticResource OtpTextBoxStyle}" Margin="0,0,10.5,0"/>
                    <TextBox Style="{StaticResource OtpTextBoxStyle}" Margin="0,0,10.5,0"/>
                    <TextBox Style="{StaticResource OtpTextBoxStyle}" Margin="0,0,10.5,0"/>
                    <TextBox Style="{StaticResource OtpTextBoxStyle}" Margin="0,0,10.5,0"/>
                    <TextBox Style="{StaticResource OtpTextBoxStyle}" Margin="0,0,10.5,0"/>
                    <TextBox Style="{StaticResource OtpTextBoxStyle}"/>
                </StackPanel>

                <!-- Nút Xác thực mã -->
                <Button Content="Xác thực mã" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,20"/>

                <!-- Nút Trở về trang đăng nhập -->
                <Button Content="Trở về trang đăng nhập" Style="{StaticResource LinkButtonStyle}" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>