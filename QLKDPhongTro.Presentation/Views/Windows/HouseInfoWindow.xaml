<Window
    x:Class="QLKDPhongTro.Presentation.Views.Windows.HouseInfoWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Thông tin nhà"
    Height="420"
    Width="650"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Icon="/Resources/Images/Logo.png"
    MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#3B4758"/>
            <Setter Property="Margin" Value="0,12,0,5"/>
        </Style>

        <Style x:Key="InputFieldStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="#F2F6FA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="4" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="White" Margin="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="#CCCCCC" Opacity="0.5" BlurRadius="10"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Grid Grid.Row="0" Margin="24,24,24,20">
                <TextBlock Text="Thông tin nhà" FontSize="18" FontWeight="Medium" Foreground="#3B4758" VerticalAlignment="Center"/>
                <Button x:Name="CloseButton" Click="CloseButton_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Width="32" Height="32" BorderThickness="0" Background="Transparent" Foreground="#586A84" ToolTip="Đóng">
                    <TextBlock Text="✕" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Button>
            </Grid>

            <!-- CONTENT -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24,0,24,24">
                <StackPanel>
                    <TextBlock Text="Mã nhà" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="MaNhaTextBox" Style="{StaticResource InputFieldStyle}" IsReadOnly="True" Background="#F9FAFB" Foreground="#6B7280"/>

                    <TextBlock Text="Địa chỉ" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="DiaChiTextBox" Style="{StaticResource InputFieldStyle}"/>

                    <TextBlock Text="Tỉnh / Thành phố" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="TinhThanhTextBox" Style="{StaticResource InputFieldStyle}"/>

                    <TextBlock Text="Tổng số phòng" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="TongSoPhongTextBox" Style="{StaticResource InputFieldStyle}"/>
                    <TextBlock Text="Tối đa 10 phòng" Margin="0,2,0,0" FontSize="11" Foreground="#888"/>

                    <TextBlock Text="Ghi chú" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="GhiChuTextBox" Style="{StaticResource InputFieldStyle}" AcceptsReturn="True" Height="80" TextWrapping="Wrap"/>

                    <Button x:Name="SaveButton" Content="Lưu thông tin" Background="#319DFF" Foreground="White" Width="170" Height="45" FontSize="16" FontWeight="SemiBold" BorderThickness="0" HorizontalAlignment="Right" Margin="0,25,0,0" Cursor="Hand" Click="SaveButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="6" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
