<Window x:Class="QLKDPhongTro.Presentation.Views.Windows.PaymentFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:QLKDPhongTro.Presentation.ViewModels"
        xmlns:converters="clr-namespace:QLKDPhongTro.Presentation.Converters"
        Title="Ghi nhận thanh toán - Quản Lý Phòng Trọ"
        Height="550"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Icon="/Resources/Images/Logo.png">

    <Window.DataContext>
        <viewModels:PaymentFormViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:NumberFormatConverter x:Key="NumberFormatConverter"/>

        <!-- Color palette -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#10B981"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#059669"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#3B82F6"/>
        <SolidColorBrush x:Key="SecondaryDarkColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="AccentColor" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280"/>
        <SolidColorBrush x:Key="TextTertiary" Color="#9CA3AF"/>
        <SolidColorBrush x:Key="BackgroundLight" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BackgroundLighter" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="BackgroundCard" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="BorderLightColor" Color="#F3F4F6"/>

        <!-- Shadows -->
        <DropShadowEffect x:Key="CardShadow" Color="#000000" Opacity="0.06" ShadowDepth="2" BlurRadius="12" Direction="270"/>
        <DropShadowEffect x:Key="HoverShadow" Color="#000000" Opacity="0.12" ShadowDepth="4" BlurRadius="16" Direction="270"/>
        <DropShadowEffect x:Key="ButtonShadow" Color="#000000" Opacity="0.1" ShadowDepth="1" BlurRadius="4" Direction="270"/>

        <!-- Modern Button Style -->
        <Style TargetType="{x:Type Button}" x:Key="ModernPrimaryButton">
            <Setter Property="Background" Value="{StaticResource SecondaryGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" CornerRadius="8" Background="{TemplateBinding Background}" Effect="{StaticResource ButtonShadow}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Effect" Value="{StaticResource HoverShadow}"/>
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SecondaryDarkColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style TargetType="{x:Type Button}" x:Key="ModernSecondaryButton">
            <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundLighter}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho textbox -->
        <Style TargetType="{x:Type TextBox}" x:Key="FormTextBoxStyle">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho combobox -->
        <Style TargetType="{x:Type ComboBox}" x:Key="FormComboBoxStyle">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                </Trigger>
                <Trigger Property="IsDropDownOpen" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho DatePicker -->
        <Style TargetType="{x:Type DatePicker}" x:Key="FormDatePickerStyle">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="💰 Ghi nhận thanh toán"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Thêm khoản thanh toán tiền thuê mới"
                       FontSize="14"
                       Foreground="{StaticResource TextSecondary}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Form Content -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="12"
                BorderThickness="1"
                BorderBrush="{StaticResource BorderColor}"
                Effect="{StaticResource CardShadow}"
                Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Chọn hợp đồng -->
                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="Hợp đồng:"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,15,15"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          Style="{StaticResource FormComboBoxStyle}"
                          ItemsSource="{Binding Contracts}"
                          DisplayMemberPath="TenPhong"
                          SelectedItem="{Binding SelectedContract}"
                          Margin="0,0,0,15"/>

                <!-- Tháng/Năm -->
                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="Tháng/Năm:"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,15,15"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Style="{StaticResource FormTextBoxStyle}"
                         Text="{Binding ThangNam}"
                         Margin="0,0,0,15"
                         ToolTip="Định dạng: MM/yyyy (ví dụ: 12/2024)"/>

                <!-- Số tiền -->
                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="Số tiền:"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,15,15"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Style="{StaticResource FormTextBoxStyle}"
                         Text="{Binding Amount, Converter={StaticResource NumberFormatConverter}, UpdateSourceTrigger=PropertyChanged}"
                         TextAlignment="Right"
                         Margin="0,0,0,15"/>

                <!-- Ngày thanh toán -->
                <TextBlock Grid.Row="3" Grid.Column="0"
                           Text="Ngày thanh toán:"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,15,15"/>
                <DatePicker Grid.Row="3" Grid.Column="1"
                            Style="{StaticResource FormDatePickerStyle}"
                            SelectedDate="{Binding PaymentDate}"
                            Margin="0,0,0,15"/>

                <!-- Phương thức thanh toán -->
                <TextBlock Grid.Row="4" Grid.Column="0"
                           Text="Phương thức:"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,15,15"/>
                <ComboBox Grid.Row="4" Grid.Column="1"
                          Style="{StaticResource FormComboBoxStyle}"
                          Text="{Binding PaymentMethod}"
                          Margin="0,0,0,15">
                    <ComboBoxItem Content="Tiền mặt"/>
                    <ComboBoxItem Content="Chuyển khoản"/>
                    <ComboBoxItem Content="Thẻ ngân hàng"/>
                </ComboBox>

                <!-- Thông tin hợp đồng -->
                <Border Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                        Background="{StaticResource BackgroundLighter}"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,15"
                        Visibility="{Binding SelectedContract, Converter={StaticResource NullToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Thông tin hợp đồng:"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"
                                   Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding SelectedContract.TenNguoiThue, StringFormat='Người thuê: {0}'}"
                                           FontSize="13"
                                           Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="{Binding SelectedContract.TenPhong, StringFormat='Phòng: {0}'}"
                                           FontSize="13"
                                           Foreground="{StaticResource TextSecondary}"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding SelectedContract.NgayBatDau, StringFormat='Ngày bắt đầu: {0:dd/MM/yyyy}'}"
                                           FontSize="13"
                                           Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="{Binding SelectedContract.NgayKetThuc, StringFormat='Ngày kết thúc: {0:dd/MM/yyyy}'}"
                                           FontSize="13"
                                           Foreground="{StaticResource TextSecondary}"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Ghi chú -->
                <TextBlock Grid.Row="6" Grid.Column="0"
                           Text="Ghi chú:"
                           VerticalAlignment="Top"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,10,15,0"/>
                <TextBox Grid.Row="6" Grid.Column="1"
                         Height="80"
                         Padding="12"
                         VerticalContentAlignment="Top"
                         TextWrapping="Wrap"
                         BorderBrush="{StaticResource BorderColor}"
                         BorderThickness="1"
                         Background="White"
                         FontSize="14"
                         Foreground="{StaticResource TextPrimary}"
                         Text="{Binding Notes}"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Hủy"
                    Width="100"
                    Style="{StaticResource ModernSecondaryButton}"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Lưu thanh toán"
                    Width="140"
                    Style="{StaticResource ModernPrimaryButton}"
                    Background="{StaticResource PrimaryColor}"
                    Margin="12,0,0,0"
                    Command="{Binding SavePaymentCommand}">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>