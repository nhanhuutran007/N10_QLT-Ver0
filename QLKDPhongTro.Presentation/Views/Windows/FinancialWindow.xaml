<Window x:Class="QLKDPhongTro.Presentation.Views.Windows.FinancialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:components="clr-namespace:QLKDPhongTro.Presentation.Views.Components"
        xmlns:local="clr-namespace:QLKDPhongTro.Presentation.Views.Windows"
        xmlns:converters="clr-namespace:QLKDPhongTro.Presentation.Converters"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Quản lý tài chính - Quản Lý Phòng Trọ"
        MinHeight="600"
        MinWidth="900"
        Height="720"
        Width="1280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Icon="/Resources/Images/Logo.png">

    <Window.Resources>
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:PageNumberEqualsMultiConverter x:Key="PageNumberEqualsMultiConverter"/>
        <converters:NumberFormatConverter x:Key="NumberFormatConverter"/>

        <!-- === STYLES CẢI TIẾN HIỆN ĐẠI === -->

        <!-- Color palette -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#10B981"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#059669"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#3B82F6"/>
        <SolidColorBrush x:Key="SecondaryDarkColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="AccentColor" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280"/>
        <SolidColorBrush x:Key="TextTertiary" Color="#9CA3AF"/>
        <SolidColorBrush x:Key="BackgroundLight" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BackgroundLighter" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="BackgroundCard" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="BorderLightColor" Color="#F3F4F6"/>

        <!-- Gradients -->
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#10B981" Offset="0"/>
            <GradientStop Color="#059669" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="SecondaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#3B82F6" Offset="0"/>
            <GradientStop Color="#2563EB" Offset="1"/>
        </LinearGradientBrush>

        <!-- Shadows -->
        <DropShadowEffect x:Key="CardShadow" Color="#000000" Opacity="0.06" ShadowDepth="2" BlurRadius="12" Direction="270"/>
        <DropShadowEffect x:Key="HoverShadow" Color="#000000" Opacity="0.12" ShadowDepth="4" BlurRadius="16" Direction="270"/>
        <DropShadowEffect x:Key="ButtonShadow" Color="#000000" Opacity="0.1" ShadowDepth="1" BlurRadius="4" Direction="270"/>

        <!-- Modern Button Style -->
        <Style TargetType="{x:Type Button}" x:Key="ModernPrimaryButton">
            <Setter Property="Background" Value="{StaticResource SecondaryGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" CornerRadius="8" Background="{TemplateBinding Background}" Effect="{StaticResource ButtonShadow}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Effect" Value="{StaticResource HoverShadow}"/>
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SecondaryDarkColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Scan Button Style -->
        <Style TargetType="{x:Type Button}" x:Key="ScanButtonStyle" BasedOn="{StaticResource ModernPrimaryButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryGradient}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                    <!-- ADD: Thêm hiệu ứng shadow khi hover cho nhất quán -->
                    <Setter Property="Effect" Value="{StaticResource HoverShadow}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox Styles (Simplified for brevity but modern) -->
        <Style TargetType="{x:Type ComboBox}" x:Key="ComboBoxStyle">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <Grid>
                                <ToggleButton Name="ToggleButton" Template="{DynamicResource ComboBoxToggleButton}" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
                                <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}" Template="{x:Null}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,30,0" Focusable="True" Background="Transparent" Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="{StaticResource BorderColor}" CornerRadius="6" Effect="{StaticResource CardShadow}" Margin="0,2,0,0">
                                            <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                        <!-- ADD: Thêm trigger cho hover và focus -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Helper for ComboBox Toggle Button -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="6" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" />
                <Path x:Name="Arrow" Grid.Column="1" Fill="{StaticResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>

        <!-- Segment Button Styles -->
        <Style TargetType="{x:Type Button}" x:Key="ModernSegmentButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Button}" x:Key="ModernActiveSegmentButton" BasedOn="{StaticResource ModernSegmentButton}">
            <Setter Property="Background" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Effect" Value="{StaticResource ButtonShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style -->
        <Style TargetType="{x:Type Border}" x:Key="ModernFinanceCard">
            <Setter Property="Background" Value="{StaticResource BackgroundCard}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLightColor}"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <!-- ADD: Style Triggers cho hover effect -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                    <Setter Property="Effect" Value="{StaticResource HoverShadow}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Styles (NEW & IMPROVED) -->
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Background" Value="{StaticResource BackgroundLighter}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#F0F9FF"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLightColor}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F9FAFB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type ProgressBar}" x:Key="ModernProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryColor}"/>
        </Style>

    </Window.Resources>

    <Grid Background="{StaticResource BackgroundLighter}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" MinWidth="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <components:SidebarControl Grid.Column="0" x:Name="SidebarControl"/>

        <!-- MAIN CONTENT -->
        <Grid Margin="20" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Top Bar -->
                <RowDefinition Height="Auto"/>
                <!-- Title & Tabs -->
                <RowDefinition Height="*"/>
                <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <components:TopbarControl Grid.Row="0" x:Name="TopbarControl" Margin="0,0,0,20"/>

            <!-- Content Area -->
            <Grid Grid.Row="1" Grid.RowSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header Section -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="Quản lý tài chính" Foreground="{StaticResource TextPrimary}" FontSize="28" FontWeight="Bold" Margin="0,0,0,24"/>

                    <!-- Action Bar & Tabs Container -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Tabs -->
                        <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="4" BorderThickness="1" BorderBrush="{StaticResource BorderColor}" Effect="{StaticResource CardShadow}">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Tất cả bản ghi" 
                                        x:Name="ViewAllRecordsButton"
                                        Click="ViewAllRecords_Click"
                                        Style="{StaticResource ModernActiveSegmentButton}"
                                        Width="140"/>
                                <Button Content="Quản lý công nợ" 
                                        x:Name="ViewDebtsButton"
                                        Click="ViewDebts_Click"
                                        Style="{StaticResource ModernSegmentButton}"
                                        Width="140"/>
                                <!-- REMOVED: Báo cáo thu chi Tab -->
                            </StackPanel>
                        </Border>

                        <!-- Actions -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <!-- REMOVED: Nhập tay Button -->
                            <Button Content="Đồng bộ G.Form"
                                    Style="{StaticResource ScanButtonStyle}"
                                    Command="{Binding ProcessDebtsFromGoogleFormCommand}"
                                    Width="150" Margin="0,0,12,0"/>

                            <!-- Search Box -->
                            <Border Background="White" CornerRadius="8" BorderThickness="1" Width="240" Height="40" Margin="0,0,12,0">
                                <!-- ADD: Style cho Border để thay đổi màu khi TextBox focus -->
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=FinancialSearchTextBox}" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <TextBlock Text="Tìm kiếm..." Foreground="{StaticResource TextSecondary}" FontSize="14" IsHitTestVisible="False" VerticalAlignment="Center" Margin="12,0,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=FinancialSearchTextBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBox x:Name="FinancialSearchTextBox" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Margin="12,0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                            </Border>

                            <ComboBox Width="140" Style="{StaticResource ComboBoxStyle}" SelectedIndex="0">
                                <ComboBoxItem>Mới nhất</ComboBoxItem>
                                <ComboBoxItem>Cũ nhất</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Fee configuration -->
                    <Border Background="White"
                            CornerRadius="10"
                            Padding="16"
                            Margin="0,0,0,20"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderColor}"
                            Effect="{StaticResource CardShadow}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Thiết lập phí cố định trước khi tạo công nợ"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimary}"/>
                                <TextBlock Text=" (áp dụng cho lần xử lý tiếp theo)"
                                           FontSize="13"
                                           Foreground="{StaticResource TextSecondary}"
                                           Margin="6,0,0,0"/>
                            </StackPanel>

                            <UniformGrid Columns="3" Margin="0,12,0,0" HorizontalAlignment="Stretch">
                                <Border Background="{StaticResource BackgroundLighter}" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="Tiền nước (VND)" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6"/>
                                        <TextBox Text="{Binding WaterFeeInput, Converter={StaticResource NumberFormatConverter}, UpdateSourceTrigger=PropertyChanged}"
                                                 Height="36"
                                                 VerticalContentAlignment="Center"
                                                 BorderBrush="{StaticResource BorderColor}"
                                                 BorderThickness="1"
                                                 Padding="10,0"
                                                 Background="White"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{StaticResource BackgroundLighter}" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="Tiền internet (VND)" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6"/>
                                        <TextBox Text="{Binding InternetFeeInput, Converter={StaticResource NumberFormatConverter}, UpdateSourceTrigger=PropertyChanged}"
                                                 Height="36"
                                                 VerticalContentAlignment="Center"
                                                 BorderBrush="{StaticResource BorderColor}"
                                                 BorderThickness="1"
                                                 Padding="10,0"
                                                 Background="White"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{StaticResource BackgroundLighter}" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="Tiền vệ sinh (VND)" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6"/>
                                        <TextBox Text="{Binding SanitationFeeInput, Converter={StaticResource NumberFormatConverter}, UpdateSourceTrigger=PropertyChanged}"
                                                 Height="36"
                                                 VerticalContentAlignment="Center"
                                                 BorderBrush="{StaticResource BorderColor}"
                                                 BorderThickness="1"
                                                 Padding="10,0"
                                                 Background="White"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <TextBlock Text="Các giá trị trên sẽ được ghi nhớ cho tới khi bạn thay đổi và được áp dụng khi đồng bộ dữ liệu Google Form."
                                       FontSize="12"
                                       Foreground="{StaticResource TextSecondary}"
                                       Margin="0,12,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Main Views -->
                <Grid Grid.Row="1">
                    <!-- Loading Indicator -->
                    <Border Background="White" CornerRadius="12" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                            BorderThickness="1" BorderBrush="{StaticResource BorderLightColor}" Effect="{StaticResource CardShadow}" Panel.ZIndex="99">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ProgressBar IsIndeterminate="True" Width="24" Height="24" Margin="0,0,16,0" Foreground="{StaticResource SecondaryColor}" Style="{StaticResource ModernProgressBar}"/>
                            <TextBlock Text="Đang tải dữ liệu..." FontSize="15" Foreground="{StaticResource TextSecondary}" FontWeight="Medium" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- VIEW: TẤT CẢ BẢN GHI -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="{Binding IsAllRecordsView, Converter={StaticResource BooleanToVisibilityConverter}}" Padding="0,0,6,0">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding FinancialRecords}" Margin="0,0,0,20">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <!-- REMOVE: Loại bỏ MouseEnter/MouseLeave, dùng Style Trigger -->
                                        <Border Style="{StaticResource ModernFinanceCard}" Width="320" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}" Cursor="Hand">
                                            <Border.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="Xem chi tiết" Command="{Binding PlacementTarget.Tag.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                    <MenuItem Header="Đánh dấu đã trả" Command="{Binding PlacementTarget.Tag.MarkAsPaidCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                    <Separator/>
                                                    <MenuItem Header="Chỉnh sửa" Command="{Binding PlacementTarget.Tag.EditCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                    <MenuItem Header="Xóa" Command="{Binding PlacementTarget.Tag.DeleteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Foreground="{StaticResource ErrorColor}"/>
                                                </ContextMenu>
                                            </Border.ContextMenu>

                                            <StackPanel>
                                                <Border Background="{StaticResource BackgroundLighter}" Padding="20,16" CornerRadius="12,12,0,0" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderLightColor}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding TenPhong}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextPrimary}"/>
                                                            <TextBlock Text="{Binding KyHan, StringFormat='Kỳ: {0:MMM yyyy}'}" FontSize="13" Foreground="{StaticResource TextTertiary}" Margin="0,4,0,0"/>
                                                        </StackPanel>
                                                        <Border Grid.Column="1" Background="White" CornerRadius="6" Padding="8,4" BorderThickness="1" BorderBrush="{StaticResource BorderColor}">
                                                            <TextBlock Text="{Binding LoaiGiaoDich}" FontSize="12" Foreground="{StaticResource TextSecondary}" FontWeight="Medium"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>

                                                <StackPanel Margin="20">
                                                    <StackPanel Margin="0,0,0,16">
                                                        <Grid Margin="0,0,0,8">
                                                            <TextBlock Text="Số tiền" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                                                            <TextBlock Text="{Binding TongTien, StringFormat='{}{0:N0} VNĐ'}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                                                        </Grid>
                                                        <Grid>
                                                            <TextBlock Text="Trạng thái" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                                                            <TextBlock Text="{Binding TrangThaiText}" FontSize="14" FontWeight="SemiBold" Foreground="{Binding TrangThaiColor}" HorizontalAlignment="Right"/>
                                                        </Grid>
                                                    </StackPanel>

                                                    <ProgressBar Style="{StaticResource ModernProgressBar}" Value="{Binding ProgressValue}" Margin="0,0,0,12" Minimum="0" Maximum="100"/>
                                                    <TextBlock Text="Xem chi tiết" Foreground="{StaticResource SecondaryColor}" FontSize="14" FontWeight="Medium" HorizontalAlignment="Right" Cursor="Hand" MouseLeftButtonDown="ViewDetail_MouseLeftButtonDown"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Pagination -->
                            <Border Background="White" CornerRadius="10" Padding="16" BorderThickness="1" BorderBrush="{StaticResource BorderColor}" Effect="{StaticResource CardShadow}" Margin="0,0,0,20">
                                <Grid>
                                    <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="{StaticResource TextSecondary}" FontSize="14"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="Trước" Style="{StaticResource ModernSegmentButton}" Command="{Binding PreviousPageCommand}" IsEnabled="{Binding CanGoToPreviousPage}" Margin="0,0,8,0"/>
                                        <ItemsControl ItemsSource="{Binding PageNumbers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding}" Width="36" Height="36" Margin="4,0" Click="PageNumber_Click" CommandParameter="{Binding}">
                                                        <Button.Style>
                                                            <Style TargetType="Button" BasedOn="{StaticResource ModernSegmentButton}">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Value="True">
                                                                        <DataTrigger.Binding>
                                                                            <MultiBinding Converter="{StaticResource PageNumberEqualsMultiConverter}">
                                                                                <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DataContext.CurrentPage" />
                                                                                <Binding />
                                                                            </MultiBinding>
                                                                        </DataTrigger.Binding>
                                                                        <Setter Property="Background" Value="{StaticResource TextPrimary}"/>
                                                                        <Setter Property="Foreground" Value="White"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <Button Content="Sau" Style="{StaticResource ModernSegmentButton}" Command="{Binding NextPageCommand}" IsEnabled="{Binding CanGoToNextPage}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- VIEW: QUẢN LÝ CÔNG NỢ -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="{Binding IsDebtsView, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Border Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource BorderColor}" Effect="{StaticResource CardShadow}" Margin="0,0,8,20" Padding="0">
                            <StackPanel>
                                <TextBlock Text="Danh sách công nợ" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="24,24,0,24"/>

                                <DataGrid ItemsSource="{Binding Debts}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" BorderThickness="0" GridLinesVisibility="None" HeadersVisibility="Column" RowHeight="60" HorizontalAlignment="Stretch" Padding="0,0,0,12">
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                            <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Cảnh báo">
                                                    <Setter Property="Background" Value="#FEF2F2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.RowStyle>

                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Phòng" Binding="{Binding TenPhong}" Width="100" FontWeight="Bold"/>
                                        <DataGridTextColumn Header="Số liệu (Nhập | Ảnh)" Binding="{Binding ComparisonInfo}" Width="220"/>

                                        <DataGridTemplateColumn Header="Ghi chú hệ thống" Width="*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding DiaChi}" TextWrapping="Wrap" VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Cảnh báo">
                                                                        <Setter Property="Foreground" Value="{StaticResource ErrorColor}"/>
                                                                        <Setter Property="FontWeight" Value="Medium"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTextColumn Header="Tổng tiền dự kiến" Binding="{Binding TongTien, StringFormat='{}{0:N0} VNĐ'}" Width="160" FontWeight="SemiBold"/>

                                        <DataGridTemplateColumn Header="Trạng thái" Width="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Border CornerRadius="14" Padding="12,6" HorizontalAlignment="Left">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="#F3F4F6"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Cảnh báo">
                                                                        <Setter Property="Background" Value="#FEE2E2"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Chờ duyệt">
                                                                        <Setter Property="Background" Value="#D1FAE5"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Đã xác nhận">
                                                                        <Setter Property="Background" Value="#DBEAFE"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding TrangThaiThanhToan}" FontWeight="SemiBold" FontSize="12">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="#374151"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Cảnh báo">
                                                                            <Setter Property="Foreground" Value="#B91C1C"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Chờ duyệt">
                                                                            <Setter Property="Foreground" Value="#047857"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding TrangThaiThanhToan}" Value="Đã xác nhận">
                                                                            <Setter Property="Foreground" Value="#1D4ED8"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Border>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Width="80">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Sửa" Command="{Binding DataContext.InspectDiscrepancyCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Style="{StaticResource ModernSegmentButton}" Padding="0" Height="32" FontSize="13"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </ScrollViewer>

                    <!-- REMOVED: VIEW: BÁO CÁO THU CHI -->

                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>